# src

后续自写代码放这里。

建议等脚本和实验跑通后，再沉淀为 Python 包或 CUDA demo。初期不要过早抽象。

可能的子方向：

- benchmark runner；
- result parser；
- plot generator；
- KV cache 实验代码；
- speculative decoding 小复现；
- RMSNorm / RoPE / INT4 matmul CUDA demo。
